/**
 * @param {string} text
 * @param {string} pattern
 * @return {number}
 */
var maximumSubsequenceCount = function (text, pattern) {
    let aCnt = 0, bCnt = 0
    
};

var text = "abdcdbc", pattern = "ac"

let res = maximumSubsequenceCount(text, pattern)
console.log('res', res)