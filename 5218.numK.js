/**
 * @param {number} num
 * @param {number} k
 * @return {number}
 */
var minimumNumbers = function (num, k) {
    if (num % 2 !== 0 && k % 2 === 0) return -1
    
};


var num = 58, k = 9

minimumNumbers(num, k)